spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: abc123
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop # makes schema changes to the DB to match hibernate entities
    properties:
      hibernate:
        default_schema: test